# opensoyce-wire0-test

Scratch repo for live-testing OpenSoyce's `pr-walkthrough` GitHub Action — specifically the zero-custody delivery path (PHASE0-WIRE-0): does a real PR in a real *public* repo get a working `?src=` viewer link, fetched directly by the browser with zero requests to any OpenSoyce origin?

Safe to delete once verified.
